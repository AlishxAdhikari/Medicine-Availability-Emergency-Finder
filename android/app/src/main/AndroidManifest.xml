<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Nearest-pharmacy / nearest-blood-bank search is distance-sorted around
         the user, so it needs a real fix. FINE is requested because COARSE
         (~1-3km on its own) is the same order of magnitude as the 5km minimum
         search radius, which would make the ordering meaningless. COARSE is
         declared too so the app still works when the user grants only
         approximate location on Android 12+. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- CALL_PHONE is deliberately NOT requested. The emergency buttons open
         the dialer with the number pre-filled (ACTION_DIAL) rather than
         placing the call themselves, so the user always gets a last look
         before an emergency line rings. -->

    <!-- usesCleartextTraffic is required because the app talks to the Django
         dev server over plain http:// and ws:// on the LAN. Android blocks
         cleartext by default from targetSdk 28 onwards, and the failure is
         silent: every request fails exactly as if the server were down. The
         iOS counterpart is NSAllowsLocalNetworking in ios/Runner/Info.plist.
         Both should be removed once the backend is served over HTTPS. -->
    <application
        android:label="medalert"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility. Without these, canLaunchUrl()
             returns false for tel:/geo:/https: even when a dialer or maps app
             is installed, and every Call and Directions button would report
             "no app available" on a perfectly capable phone. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
